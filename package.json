{
  "name": "afternoon-code",
  "version": "1.0.0",
  "description": "- Connect a local mongodb database to express  - Create a db on the mongo command line, create a user collection, then add in four user objects, you can use what's below",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.6.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
